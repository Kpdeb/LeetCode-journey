
 while (st.find(x + 1) != st.end()) {
                    x++;
                    cnt++;
                }
 longest = max(longest, cnt);
            }
        }

output :

Input
nums =
[100,4,200,1,3,2]
Output
4
Expected
4


        return longest;
    }
};
